# Headless service for stable DNS entries of StatefulSet members.
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.mysql.namespace.name }}
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
  - name: {{ .Chart.Name }}
    port: 3306
  clusterIP: None
